<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


        <!-- "Mark Favorite", should appear as action button if possible -->
        <item
            android:id="@+id/action_destination"
            android:icon="@drawable/directions_walk_white_24dp"
            android:title="Destination"
            app:showAsAction="ifRoom"/>
        <item
            android:id="@+id/action_del_markers"
            android:icon="@drawable/musorka"
            android:title="Delete markers"
            app:showAsAction="ifRoom"/>
        <item
            android:id="@+id/action_send"
            android:icon="@drawable/play_gray"
            android:title="Send"
            app:showAsAction="ifRoom"/>
        <item
            android:id="@+id/action_back"
            android:icon="@drawable/exit_grey"
            android:title="Back"
            app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
        <item android:id="@+id/action_settings"
            android:title="@string/action_settings"
            app:showAsAction="never"/>


</menu>